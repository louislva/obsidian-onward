{
  "name": "obsidian-inline-complete",
  "version": "0.4.0",
  "description": "Copilot-style inline prose completion for Obsidian via Tinker and OpenRouter.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run",
    "check": "tsc --noEmit && npm test && npm run build"
  },
  "devDependencies": {
    "@codemirror/state": "6.5.0",
    "@codemirror/view": "6.38.6",
    "@types/node": "^24.0.10",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.25.6",
    "obsidian": "^1.8.7",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  }
}
